<?php
  namespace ThemeClasses\Handlers;

  class _Core {
    public function __construct() {
      // new GoogleReviews();
      // new YoutubeVideos();
      // new InstagramPosts();
      // new Map();
    }
  }